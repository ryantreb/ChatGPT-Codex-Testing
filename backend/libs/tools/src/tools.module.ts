import { Module } from '@nestjs/common';

// Tools module handles integration with external tools (SIEM, EDR, ticketing, etc.)
@Module({
  providers: [],
  exports: [],
})
export class ToolsModule {}
